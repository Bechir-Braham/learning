- name: My first play
  hosts: myhosts
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:

   - name: Print message
     ansible.builtin.debug:
       msg: Hello world

   - name: potato
     ansible.builtin.template:
       src: /home/bb/ansible/Dockerfile
       dest: /home/bb
